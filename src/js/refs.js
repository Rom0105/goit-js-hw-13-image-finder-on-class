const searchForms = document.querySelector('.search-form');
const articlesContainer = document.querySelector('.gallery');
const loadMore = document.querySelector('[data-action="load-more"]');
const input = document.querySelector('.input-form');
const btn = document.querySelector('.btn');
const element = document.getElementById('my-element-selector');
const isHiddenBtn = document.querySelector('.ishidden');

export { searchForms, articlesContainer, loadMore, input, btn, element, isHiddenBtn };
